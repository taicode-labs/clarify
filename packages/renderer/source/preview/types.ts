export type PreviewGuide = {
  label: string
  title: string
  body: string
  embed: string
  outputs?: string[]
}

export type PreviewMetric = {
  label: string
  value: string
  hint?: string
}

export type PreviewCard = {
  title: string
  description: string
}
